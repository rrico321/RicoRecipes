---
phase: 01-foundations
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .github/workflows/ci.yml
  - turbo.json
autonomous: false
tags: [ci, vercel, neon, turbo-remote-cache, deploy]
requirements: [FOUND-02, FOUND-03, FOUND-04]
user_setup:
  - service: github
    why: "FOUND-03 (CI on every PR) requires the repo to live on GitHub; FOUND-04 (preview deploy) requires Vercel Git Integration which only works against a Git remote"
    dashboard_config:
      - task: "Confirm or create the GitHub repository for rico_recipe and push the local repo to it"
        location: "github.com → New repository, then `git remote add origin … && git push -u origin main`"
  - service: vercel
    why: "FOUND-02 (Vercel project linked) and FOUND-04 (preview deploy of apps/mcp)"
    env_vars:
      - name: TURBO_TOKEN
        source: "Vercel Dashboard → Settings → Tokens → Create — used as a GitHub Actions secret to unlock Turbo remote cache"
      - name: TURBO_TEAM
        source: "Vercel team slug (visible in URL bar at vercel.com/<team-slug>) — used as a GitHub Actions repository variable"
    dashboard_config:
      - task: "Confirm Vercel team membership; install Vercel CLI; run `vercel whoami` to verify the right team"
        location: "vercel.com (browser) + local terminal"
      - task: "Run `vercel link --repo` from repo root to map apps/mcp → its own Vercel project (creates .vercel/repo.json — gitignored, per-developer local artifact, NOT tracked by this plan). User chooses project slug (e.g., rico-mcp)"
        location: "local terminal in repo root"
      - task: "Set the Vercel project's Root Directory = apps/mcp in Project Settings → Build and Deployment"
        location: "Vercel Dashboard → <project> → Settings → Build and Deployment"
  - service: neon
    why: "FOUND-02 (Neon Postgres provisioned and DATABASE_URL propagated to apps/mcp Vercel project)"
    dashboard_config:
      - task: "From the apps/mcp Vercel project: Storage tab → Create Database → Neon Postgres (Vercel-managed). Auto-provisions Neon project and injects DATABASE_URL into Development/Preview/Production env scopes"
        location: "Vercel Dashboard → <project> → Storage"
  - service: github-actions
    why: "Turbo remote cache requires CI secrets; FOUND-03 requires CI to run on every PR"
    env_vars:
      - name: TURBO_TOKEN
        source: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret"
      - name: TURBO_TEAM
        source: "GitHub repo → Settings → Secrets and variables → Actions → Variables tab → New repository variable"
must_haves:
  truths:
    - "Pushing a PR triggers .github/workflows/ci.yml which runs build + typecheck + test + lint via Turbo and blocks merge on failure (all four gates from ROADMAP success criterion #1)"
    - "Vercel project for apps/mcp exists and is linked via `vercel link --repo` (.vercel/repo.json present locally; gitignored)"
    - "DATABASE_URL appears in `vercel env ls` for the apps/mcp project across Development, Preview, Production"
    - "A preview deployment of apps/mcp returns HTTP 200 to `curl https://<preview-url>/api/health` from the public internet"
    - "Turbo remote cache is wired: TURBO_TOKEN secret + TURBO_TEAM variable set in GitHub Actions; local `turbo link` complete"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Four-gate CI workflow (build/typecheck/test/lint via Turbo) with pnpm + Node 22 + Turbo remote cache"
      contains: "pnpm turbo run build typecheck test lint"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "GitHub repo settings → required status checks"
      via: "Branch protection rule blocks merge unless 'Build / Typecheck / Test / Lint' job passes"
      pattern: "Build.*Typecheck.*Test.*Lint|ci:"
    - from: "Vercel apps/mcp project"
      to: ".planning Neon database"
      via: "Vercel Storage → Neon native integration injects DATABASE_URL"
      pattern: "DATABASE_URL"
    - from: ".github/workflows/ci.yml"
      to: "Turbo remote cache"
      via: "TURBO_TOKEN + TURBO_TEAM env exposed to `pnpm turbo run`"
      pattern: "TURBO_TOKEN|TURBO_TEAM"
---

<objective>
Wire the deployment + CI pipeline that closes Phase 1: link Vercel + Neon to apps/mcp, configure GitHub Actions for the four gates (FOUND-03 + ROADMAP success criterion #1), enable Turborepo remote cache, and prove FOUND-04 by curling the preview URL's /api/health from the public internet.

Purpose: Closes FOUND-02 (Vercel + Neon linked), FOUND-03 (CI gates on every PR), and FOUND-04 (preview deploy 200 from public internet). All three remaining requirement IDs land here. Honors all five soft defaults from CONTEXT.md (now confirmed by RESEARCH.md): one Vercel project per deployable app via `vercel link --repo`; /api/health as the smoke target; GitHub Actions for the four gates with no Neon branching yet; `vercel env pull` workflow; Turbo remote cache from day one.
Output: A green CI workflow on a real PR + a public preview URL responding 200 to /api/health.

Note on developer-local artifacts: `vercel link --repo` writes `.vercel/repo.json`, but this file is per-developer and gitignored from Plan 01. It is NOT tracked in `files_modified` for this plan because the plan does not produce or commit it — the user creates it locally during Task 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-SKELETON.md
@.planning/phases/01-foundations/01-01-SUMMARY.md
@.planning/phases/01-foundations/01-02-SUMMARY.md

<interfaces>
<!-- Vercel monorepo linking contract (RESEARCH.md Pattern 11, cited from vercel.com/docs/cli/link + vercel.com/docs/monorepos) -->

`vercel link --repo` (run from repo root, ONCE per developer / CI runner):
- Walks the monorepo, prompts to link each detected app to a Vercel project.
- Creates .vercel/repo.json (gitignored — developer-local artifact only; not part of this plan's tracked files).
- Per project Vercel-CLI skill: this is the correct shape for monorepos with multiple deployable apps. NOT `vercel link` (which creates .vercel/project.json — wrong for monorepos).

`vercel env pull .env.local` (run from apps/mcp/):
- Pulls Vercel project env vars into apps/mcp/.env.local for local dev.

Neon Vercel-Managed integration env vars (RESEARCH.md Pattern 13, cited from neon.com/docs/guides/vercel-native-integration):
- DATABASE_URL (pooled via PgBouncer — what Drizzle/@neondatabase/serverless uses)
- DATABASE_URL_UNPOOLED (direct — for migrations)
- PGHOST, PGUSER, PGDATABASE, PGPASSWORD, PGHOST_UNPOOLED
- POSTGRES_* (legacy aliases)

Phase 1 only needs DATABASE_URL visible in `vercel env ls`. No code reads it yet.

GitHub Actions YAML contract (RESEARCH.md Pattern 10, cited from turborepo.dev/docs/guides/ci-vendors/github-actions):
- actions/checkout@v4 with fetch-depth: 2 (REQUIRED for Turbo affected-graph — RESEARCH.md Pitfall 7)
- pnpm/action-setup@v3 with version: 10 (CLAUDE.md pin)
- actions/setup-node@v4 with node-version: 22, cache: 'pnpm'
- env: TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}, TURBO_TEAM: ${{ vars.TURBO_TEAM }}
- run: pnpm install --frozen-lockfile
- run: pnpm turbo run build typecheck test lint   # all four gates per ROADMAP success criterion #1

Turbo remote cache enablement (RESEARCH.md Pattern 14):
- Local: `npx turbo login` → `npx turbo link` → pick Vercel team. Free for all Vercel users.
- CI: TURBO_TOKEN secret + TURBO_TEAM variable in repo Actions settings.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 (CHECKPOINT): Confirm prerequisite accounts and link Vercel + Neon</name>
  <what-built>Nothing yet — this checkpoint gates account/integration setup that requires the user. Per RESEARCH.md open question #1 + Environment Availability table, Vercel CLI is not installed and account/repo state is unknown. Per RESEARCH.md open question #3, the Vercel project slug is a user choice.</what-built>
  <how-to-verify>
Complete the following in order. Stop and tell Claude where you got stuck if anything fails.

PREREQUISITES (you do these — Claude cannot):

1. **GitHub repo:** Confirm rico_recipe lives on GitHub. If not: create at github.com/new (private is fine), then from local repo root: `git remote add origin git@github.com:<you>/rico_recipe.git && git push -u origin main`.

2. **Vercel CLI + team:** Install if needed and confirm the right team:
   ```
   pnpm add -g vercel@latest
   vercel login
   vercel whoami
   ```
   Tell Claude the team slug shown by `vercel whoami` — Claude needs it to set TURBO_TEAM in CI.

3. **Link the monorepo to Vercel (creates apps/mcp Vercel project):**
   ```
   cd /Users/robertrico/Desktop/rico_recipe
   vercel link --repo
   ```
   When prompted: choose the Vercel scope (your team), then for apps/mcp choose "create new project" and name it (suggestion: `rico-mcp`). Skip linking apps/web/mobile/cli for now — they'll get their own projects in Phases 4/5.
   This creates `.vercel/repo.json` (already gitignored from Plan 01). It is a developer-local artifact and is NOT committed.

4. **Set Root Directory for the Vercel project:**
   In Vercel dashboard → `rico-mcp` (or your chosen name) → Settings → Build and Deployment → Root Directory → set to `apps/mcp`. This tells Vercel which workspace to build. (Pattern 12 in RESEARCH.md.)

5. **Provision Neon + inject DATABASE_URL:**
   In Vercel dashboard → `rico-mcp` → Storage tab → Create Database → choose Neon Postgres (Vercel-managed). Accept defaults. Pick all three env scopes: Development, Preview, Production. This auto-creates a Neon project and injects DATABASE_URL etc. into the Vercel project's env vars (RESEARCH.md Pattern 13).

6. **Verify env vars propagated:**
   ```
   cd apps/mcp
   vercel env ls
   ```
   Confirm DATABASE_URL shows up in Development, Preview, Production scopes. Paste the output to Claude (with secrets redacted).

7. **Create Turbo remote cache token:**
   ```
   cd /Users/robertrico/Desktop/rico_recipe
   npx turbo login
   npx turbo link    # picks Vercel scope and links the repo to remote cache
   ```
   Then in Vercel dashboard → Settings → Tokens → Create Token (name it `turbo-ci-rico-recipe`). Copy the token value. (Pattern 14 in RESEARCH.md.)

8. **Set GitHub Actions secrets/variables:**
   In GitHub repo → Settings → Secrets and variables → Actions:
   - Secrets tab → New repository secret: name `TURBO_TOKEN`, value = the token from step 7.
   - Variables tab → New repository variable: name `TURBO_TEAM`, value = your Vercel team slug from step 2.
  </how-to-verify>
  <resume-signal>Type "linked" once steps 1–8 are complete and you have confirmed DATABASE_URL appears in `vercel env ls` for apps/mcp. If anything failed, describe what stopped.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow with four gates (build + typecheck + test + lint) and Turbo remote cache</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create the CI workflow per RESEARCH.md Pattern 10 (verbatim from turborepo.dev/docs/guides/ci-vendors/github-actions, with adaptations the researcher already made: Node 22, pnpm 10, four gates per ROADMAP success criterion #1).

`.github/workflows/ci.yml`:

- name: CI
- on: push to main; pull_request opened+synchronize
- jobs.ci:
  - name: Build / Typecheck / Test / Lint
  - timeout-minutes: 15
  - runs-on: ubuntu-latest
  - env: TURBO_TOKEN from secrets, TURBO_TEAM from vars (set in Task 1 step 8)
  - steps in order:
    1. actions/checkout@v4 with fetch-depth: 2 (RESEARCH.md Pitfall 7 — required for Turbo affected-graph; without it, cache hit-rate craters).
    2. pnpm/action-setup@v3 with version: 10 (CLAUDE.md pin; verified 10.33.0 latest).
    3. actions/setup-node@v4 with node-version: 22 (current LTS; Next 16 requires ≥ 20.9), cache: 'pnpm'.
    4. Install: `pnpm install --frozen-lockfile` (succeeds because Plan 02 committed the updated lockfile).
    5. Gates: `pnpm turbo run build typecheck test lint`. This runs all four ROADMAP success criterion #1 gates across all 10 workspaces (9 stubs from Plan 01 with no-op `build` + apps/mcp from Plan 02 with real `next build`). Turbo remote cache picks up TURBO_TOKEN/TURBO_TEAM from env automatically. Critically: `build` was added to this command in revision pass 1 to actually exercise ROADMAP criterion #1 in CI (previously CI ran only typecheck/test/lint — the `build` claim was unverified).

Do NOT add a separate `next build` step — the `build` task already covers it via the Turbo graph (apps/mcp's `build` script is `next build`; stubs are no-ops). Do NOT add Neon-branch-per-PR wiring (CONTEXT.md soft default + RESEARCH.md confirmed: Phase 2 owns that). Do NOT add `biome ci` as a separate step — `pnpm turbo run lint` invokes it through each package's lint script per Plan 01.

After writing, commit and push to a fresh branch (e.g., `phase-01/ci`) and open a PR. The PR push triggers the workflow — verify it goes green. If red, fix the underlying issue (don't loosen).
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "fetch-depth: 2" .github/workflows/ci.yml && grep -q "TURBO_TOKEN" .github/workflows/ci.yml && grep -q "TURBO_TEAM" .github/workflows/ci.yml && grep -q "pnpm turbo run build typecheck test lint" .github/workflows/ci.yml && grep -q "pnpm/action-setup@v3" .github/workflows/ci.yml && grep -q "node-version: 22" .github/workflows/ci.yml</automated>
  </verify>
  <done>ci.yml committed; pushed on a feature branch; PR opened; GitHub Actions run shows the "Build / Typecheck / Test / Lint" job green; ALL FOUR gates ran (logs show `turbo run build typecheck test lint` and apps/mcp's `next build` completed); Turbo run logs report cache write/read activity (proves TURBO_TOKEN works).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (CHECKPOINT): Verify FOUND-04 — public smoke check against the Vercel preview deploy</name>
  <what-built>Plan 02 created apps/mcp with /api/health and /api/mcp/[transport]. Task 1 linked it to a Vercel project + Neon. Task 2's PR push triggered both GitHub Actions CI AND Vercel's Git integration to build a preview deploy. This checkpoint verifies the FOUND-04 success criterion: the preview URL responds 200 to /api/health from the public internet.</what-built>
  <how-to-verify>
1. Go to the PR you opened in Task 2. Vercel posts a comment with a preview URL (something like `https://rico-mcp-git-phase-01-ci-<team>.vercel.app`). If the comment isn't there yet, wait for the Vercel build to complete (visible in the Checks tab).

2. From any terminal (your laptop, a friend's machine, anywhere on the public internet — NOT inside the Vercel network):
   ```
   curl -fsS https://<preview-url>/api/health | jq .
   ```
   Expected output:
   ```
   { "ok": true, "ts": 1747257600000 }
   ```
   (ts will be a recent timestamp.)

3. Confirm three things and report each to Claude:
   - HTTP status was 200 (curl -f ensures it errors otherwise — exit code 0 = success).
   - Response body shape matches `{ok: true, ts: <number>}`.
   - The URL is publicly reachable (no Vercel auth challenge — preview deploys are open by default unless deployment protection is enabled).

4. (Optional, bonus) Hit the MCP route to confirm it's mounted:
   ```
   curl -i -X POST https://<preview-url>/api/mcp/streamable-http \
     -H 'Content-Type: application/json' \
     -d '{}'
   ```
   You should get a non-500 response (likely 400 or 406 because the body isn't a valid MCP initialize). Phase 3 exercises the real handshake; Phase 1 just needs proof the route is mounted.

If /api/health returns anything other than 200 + the expected shape, stop and tell Claude what came back. Likely culprits: Root Directory misconfigured (Task 1 step 4 missed), DATABASE_URL not propagated (Task 1 step 6 missed — though Phase 1 doesn't read it, the Vercel build will succeed anyway), or apps/mcp build failed (check Vercel build logs).
  </how-to-verify>
  <resume-signal>Type "smoke green" with the preview URL and the curl output. Type "failed" with the error if it didn't return 200.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Turbo remote cache (Vercel) | TURBO_TOKEN crosses; auto-masked by GitHub Actions |
| Vercel build → Neon Postgres | DATABASE_URL crosses; managed by Vercel-Neon integration; never in code in Phase 1 |
| Public internet → Vercel preview URL | Anonymous traffic to /api/health (intentional) and /api/mcp/[transport] (zero tools — accepted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | TURBO_TOKEN leaked in CI logs | mitigate | Use GitHub Actions `secrets.TURBO_TOKEN` — auto-masked in logs (RESEARCH.md security domain) |
| T-03-02 | Information Disclosure | DATABASE_URL leaked via env-var dump in CI | mitigate | CI does not pull Vercel env vars; DATABASE_URL only lives in Vercel project settings + Neon-managed env scopes |
| T-03-03 | Tampering | PR with malicious workflow changes runs against TURBO_TOKEN | accept | Solo developer; PRs originate from same account. Revisit when collaborators join (likely Phase 5 or post-v1) |
| T-03-04 | Information Disclosure | .vercel/repo.json (link state) committed to git | mitigate | .gitignore from Plan 01 already excludes .vercel/ — verified; this plan does not list it in files_modified |
| T-03-05 | Elevation of Privilege | Public preview URL exposes /api/mcp/[transport] without auth | accept | Phase 1 mounts ZERO tools; nothing to elevate to. Phase 3 lands withMcpAuth + Clerk OAuth BEFORE any tool registers (CONTEXT.md decision) |
| T-03-06 | Information Disclosure | Preview URLs guessable + indexable | accept | Standard Vercel preview behavior; if posture changes, enable Vercel Deployment Protection later (Phase 3 concern) |
</threat_model>

<verification>
- `.github/workflows/ci.yml` exists with Pattern-10 shape AND runs all four gates (`build typecheck test lint`).
- A real PR was opened and the CI job went green; logs show `pnpm turbo run build typecheck test lint` succeeded across all 10 workspaces, including apps/mcp's real `next build`.
- Turbo CI logs show remote cache activity (write on first run, hit on rerun).
- `vercel env ls` for the apps/mcp project shows DATABASE_URL in Development, Preview, Production.
- `curl -fsS https://<preview-url>/api/health` returns 200 with `{ok:true,ts:<number>}` from the public internet.
- `git status --ignored` confirms .vercel/repo.json is ignored (not committed); this plan's `files_modified` does not list it.
- A second PR push triggers a fresh preview URL and the curl still passes (proves the pipeline isn't a one-off).
</verification>

<success_criteria>
ALL FOUR Phase 1 success criteria from ROADMAP.md are now demonstrable:
1. ✅ `pnpm install --frozen-lockfile` + `pnpm turbo run build typecheck test lint` succeeds across all 10 workspaces — exercised in CI by Task 2 (not just claimed in success_criteria).
2. ✅ Vercel + Neon linked via `vercel link --repo`, env vars propagated to Preview + Production.
3. ✅ Every PR triggers CI gates that block merge on failure.
4. ✅ Public preview URL of apps/mcp returns 200 to `/api/health`.

All four FOUND requirements (FOUND-01 through FOUND-04) are satisfied. Phase 1 is verifiable; ready for /gsd-verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-03-SUMMARY.md` documenting:
- Vercel project name + slug chosen by user
- Neon project ID created by the integration
- Preview URL pattern + the specific URL used for the FOUND-04 smoke check
- CI run URL (GitHub Actions) showing the green gate (link to the run that included `build` in the gate set)
- TURBO_TOKEN/TURBO_TEAM set timestamp (no values logged)
- Anything Phase 2 needs to know: the `apps/mcp` Vercel project name (so Phase 2's Drizzle migrations can target the same DATABASE_URL) and the Neon project name (so Phase 2 can configure Neon branch-per-PR there)
</output>
</output>
