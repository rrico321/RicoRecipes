---
phase: 01-foundations
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/mcp/package.json
  - apps/mcp/tsconfig.json
  - apps/mcp/next.config.ts
  - apps/mcp/next-env.d.ts
  - apps/mcp/app/layout.tsx
  - apps/mcp/app/api/mcp/[transport]/route.ts
  - apps/mcp/app/api/health/route.ts
  - apps/mcp/__tests__/health.test.ts
  - apps/mcp/.gitignore
autonomous: true
tags: [nextjs, mcp, mcp-handler, vercel]
requirements: [FOUND-01, FOUND-04]
must_haves:
  truths:
    - "apps/mcp builds via `next build` (Turbopack default in Next 16)"
    - "GET /api/health returns 200 with {ok: true, ts: number}"
    - "POST /api/mcp/[transport] is mounted via mcp-handler with zero tools and exports GET/POST/DELETE"
    - "A Vitest sanity test exercises the health route shape"
    - "apps/mcp typechecks under shared tsconfig.base.json with Next.js plugin"
  artifacts:
    - path: "apps/mcp/app/layout.tsx"
      provides: "Required Next.js 16 root layout (no UI)"
      min_lines: 5
    - path: "apps/mcp/app/api/health/route.ts"
      provides: "FOUND-04 smoke endpoint — Node runtime, returns {ok:true,ts}"
    - path: "apps/mcp/app/api/mcp/[transport]/route.ts"
      provides: "mcp-handler mount at /api/mcp/[transport], zero tools, GET/POST/DELETE"
      contains: "createMcpHandler"
    - path: "apps/mcp/next.config.ts"
      provides: "Next.js 16 config (Turbopack default, no Webpack overrides)"
    - path: "apps/mcp/__tests__/health.test.ts"
      provides: "Vitest sanity test for GET /api/health response shape"
  key_links:
    - from: "apps/mcp/app/api/mcp/[transport]/route.ts"
      to: "mcp-handler"
      via: "createMcpHandler import"
      pattern: "from ['\"]mcp-handler['\"]"
    - from: "apps/mcp/__tests__/health.test.ts"
      to: "apps/mcp/app/api/health/route.ts"
      via: "import { GET } from '../app/api/health/route'"
      pattern: "from.*api/health/route"
    - from: "apps/mcp/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends with Next plugin override"
      pattern: "extends.*tsconfig.base.json"
---

<objective>
Build apps/mcp as a real Next.js 16.2.6 App Router application — the one non-stub workspace in Phase 1. Mount mcp-handler at `/api/mcp/[transport]` with zero tools (Phase 3 owns tool definitions) and add a separate `GET /api/health` smoke endpoint that satisfies FOUND-04. Add one Vitest sanity test for /api/health to make the test gate non-trivial in apps/mcp (per RESEARCH.md open question #2 — researcher recommended yes).

Purpose: Satisfies FOUND-01 (apps/mcp workspace exists) and the deployable surface for FOUND-04 (Plan 03 wires the deploy + smoke curl). Honors locked CONTEXT.md decisions: apps/mcp is the only real Next app this phase; no UI pages; mcp-handler mount is protocol-mounted but exposes zero tools.
Output: A locally-buildable Next.js 16 app with two API routes and one passing Vitest test, plugged into the Turbo task graph from Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-SKELETON.md
@.planning/phases/01-foundations/01-01-SUMMARY.md

<interfaces>
<!-- mcp-handler API contract (RESEARCH.md Pattern 1, cited from github.com/vercel/mcp-handler README) -->

createMcpHandler signature (mcp-handler@1.1.0):
```
createMcpHandler(
  registerTools: (server: McpServer) => void,
  serverOptions: object,
  routeOptions: { basePath: string; maxDuration: number; verboseLogs: boolean }
)
```

Returns a single handler reused for GET/POST/DELETE export.

Next.js 16.2.6 Route Handler shape:
```
export async function GET(request: Request): Promise<Response>
```
- Default runtime: 'nodejs' (RESEARCH.md Architectural Responsibility Map; safe default per project Next.js skill).
- File path convention: `app/api/.../route.ts`.

Required pinned dependencies for apps/mcp (CLAUDE.md verbatim):
- next@16.2.6
- react@19.2.6
- react-dom@19.2.6
- mcp-handler@1.1.0
- @modelcontextprotocol/sdk@1.29.0

Pitfalls to honor (RESEARCH.md):
- Pitfall 3: must commit app/layout.tsx — auto-creation only happens in `next dev`, not `next build`.
- Pitfall 4: Turbopack is the default in Next 16 dev AND build — do NOT add Webpack config.
- Pitfall 5: `next build` no longer runs lint — Plan 01 + Plan 03 cover lint via biome ci.
- Pitfall 9: include DELETE in the route export — some MCP clients send DELETE on session teardown.
- Anti-pattern: do NOT add app/page.tsx (let / 404; UI is Phase 4 in apps/web).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/mcp Next.js 16 workspace (package.json, tsconfig, next.config, layout, gitignore)</name>
  <files>apps/mcp/package.json, apps/mcp/tsconfig.json, apps/mcp/next.config.ts, apps/mcp/next-env.d.ts, apps/mcp/app/layout.tsx, apps/mcp/.gitignore</files>
  <action>
Create the apps/mcp workspace as a real Next.js 16 App Router app. Replace the stub from Plan 01 only if Plan 01 created an apps/mcp stub — per CONTEXT.md, apps/mcp was excluded from stubs, so this is a fresh workspace creation.

1. `apps/mcp/package.json`: name `@rico/mcp`, version "0.0.0", private: true, type: "module".
   - Scripts:
     - "dev": "next dev --port 3001"
     - "build": "next build"
     - "start": "next start --port 3001"
     - "typecheck": "tsc --noEmit"
     - "test": "vitest run"
     - "lint": "biome check ."
   - Dependencies (CLAUDE.md pins, verbatim per RESEARCH.md):
     - next@16.2.6
     - react@19.2.6
     - react-dom@19.2.6
     - mcp-handler@1.1.0
     - @modelcontextprotocol/sdk@1.29.0
   - devDependencies:
     - @types/react@^19
     - @types/react-dom@^19
     - @types/node@^22
   - Do NOT add zod yet (Phase 3 introduces it once tools register). Do NOT add Tailwind (apps/mcp ships no UI in Phase 1).

2. `apps/mcp/tsconfig.json` (RESEARCH.md Pattern 8):
   - extends "../../tsconfig.base.json"
   - compilerOptions: plugins [{ name: "next" }], paths { "@/*": ["./*"] }, jsx "preserve", noEmit true, incremental true
   - include: ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"]
   - exclude: ["node_modules"]

3. `apps/mcp/next.config.ts`: minimal `import type { NextConfig } from 'next'; const config: NextConfig = {}; export default config;`. Do NOT add Webpack config (Pitfall 4) or experimental flags. Turbopack is on by default for dev AND build.

4. `apps/mcp/next-env.d.ts`: standard Next.js generated stub — `/// <reference types="next" />` and `/// <reference types="next/image-types/global" />`. Comment line warning not to edit.

5. `apps/mcp/app/layout.tsx` (RESEARCH.md Pattern 3, Pitfall 3): minimal RootLayout component returning `<html lang="en"><body>{children}</body></html>`. Default export only. No metadata export needed in Phase 1.

6. `apps/mcp/.gitignore`: .next/, .turbo, next-env.d.ts (Next regenerates it; standard practice), .env*.local. node_modules already covered by root.

Run `pnpm install` from repo root after creating package.json so the new workspace is registered and dependencies resolve.
  </action>
  <verify>
    <automated>cd apps/mcp && test -f package.json && test -f tsconfig.json && test -f next.config.ts && test -f app/layout.tsx && grep -q "16.2.6" package.json && grep -q "mcp-handler" package.json && grep -q '"name": "@rico/mcp"' package.json && cd ../.. && pnpm install --no-frozen-lockfile && pnpm --filter @rico/mcp exec tsc --noEmit</automated>
  </verify>
  <done>apps/mcp workspace registered in pnpm; dependencies installed at locked pins; tsc --noEmit passes; layout.tsx committed; no app/page.tsx created.</done>
</task>

<task type="auto">
  <name>Task 2: Add /api/health smoke route + /api/mcp/[transport] mcp-handler mount + Vitest sanity test</name>
  <files>apps/mcp/app/api/health/route.ts, apps/mcp/app/api/mcp/[transport]/route.ts, apps/mcp/__tests__/health.test.ts</files>
  <action>
Create the two API routes that make apps/mcp the FOUND-04 deploy target, plus one sanity test.

1. `apps/mcp/app/api/health/route.ts` (RESEARCH.md Pattern 2, satisfies FOUND-04 smoke):
   - Export `runtime = 'nodejs'` explicitly (default per Next 16, but explicit is better for FOUND-04 verification).
   - Export async GET that returns `Response.json({ ok: true, ts: Date.now() })`.
   - Per RESEARCH.md security threat T-01: return ONLY {ok, ts} — no version strings, no git SHA, no environment names. Health endpoint must not leak system info.

2. `apps/mcp/app/api/mcp/[transport]/route.ts` (RESEARCH.md Pattern 1, mcp-handler mount):
   - Import createMcpHandler from 'mcp-handler'.
   - Call createMcpHandler with: (a) tool-registration callback that registers ZERO tools (Phase 3 owns tools — leave a comment explaining), (b) empty server options object, (c) route options { basePath: '/api', maxDuration: 60, verboseLogs: false }.
   - Export the returned handler as GET, POST, AND DELETE (RESEARCH.md Pitfall 9 — DELETE needed for some MCP clients during session teardown).
   - File path uses the `[transport]` dynamic segment per CONTEXT.md locked decision (matches both Vercel docs withMcpAuth example and the mcp-handler README).

3. `apps/mcp/__tests__/health.test.ts` (per RESEARCH.md open question #2 — researcher recommended yes):
   - Vitest test file. Import { GET } from '../app/api/health/route'.
   - Test: call GET() with a stub Request, await response, assert status === 200 and body parsed JSON has shape `{ ok: true, ts: number }` with ts being a positive integer.
   - Use `await response.json()` to parse. Use Vitest's expect for assertions.
   - This test exists to make the FOUND-03 Vitest gate non-trivial in apps/mcp (otherwise it would --passWithNoTests through). Cheap insurance per researcher recommendation.

Do NOT add tests for the MCP route — exercising the Streamable HTTP initialize handshake is Phase 3 work. Do NOT add zod schemas. Do NOT register any tools.
  </action>
  <verify>
    <automated>cd apps/mcp && test -f app/api/health/route.ts && test -f "app/api/mcp/[transport]/route.ts" && test -f __tests__/health.test.ts && grep -q "createMcpHandler" "app/api/mcp/[transport]/route.ts" && grep -q "as GET, handler as POST, handler as DELETE\|as DELETE" "app/api/mcp/[transport]/route.ts" && cd ../.. && pnpm --filter @rico/mcp exec vitest run && pnpm --filter @rico/mcp exec tsc --noEmit && pnpm --filter @rico/mcp exec next build</automated>
  </verify>
  <done>health.test.ts passes under Vitest; tsc --noEmit clean; `next build` succeeds locally (Turbopack default); /api/mcp/[transport] route exports GET, POST, and DELETE; no tools registered; no app/page.tsx exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → /api/health | Anonymous traffic; exposed by design for the FOUND-04 smoke check |
| Public internet → /api/mcp/[transport] | Anonymous traffic in Phase 1 (zero tools); withMcpAuth lands in Phase 3 BEFORE any tool ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | /api/health response body | mitigate | Return only `{ok: true, ts: Date.now()}` — no version, no env, no SHA (RESEARCH.md security domain) |
| T-02-02 | Elevation of Privilege | /api/mcp/[transport] exposed without auth | accept | Phase 1 mounts ZERO tools — there is nothing to call. withMcpAuth + Clerk MCP OAuth land in Phase 3 BEFORE any tool registers (CONTEXT.md decision; RESEARCH.md security domain) |
| T-02-03 | Denial of Service | maxDuration: 60 on MCP handler | mitigate | Cap handler at 60s per Vercel docs example; prevents long-running tool calls (Phase 3 will tighten per-tool) |
| T-02-04 | Information Disclosure | verboseLogs leaking request bodies | mitigate | Set verboseLogs: false in createMcpHandler options |
</threat_model>

<verification>
- `pnpm --filter @rico/mcp exec tsc --noEmit` — clean.
- `pnpm --filter @rico/mcp exec vitest run` — health.test.ts passes (1 test, 0 failures).
- `pnpm --filter @rico/mcp exec next build` — green; produces .next/ output (Turbopack default).
- `pnpm --filter @rico/mcp exec next dev` then `curl http://localhost:3001/api/health` → 200 + `{ok:true,ts:...}` (manual local sanity; Plan 03 hits the deployed URL).
- `cat "apps/mcp/app/api/mcp/[transport]/route.ts" | grep -E "GET|POST|DELETE"` shows all three exports.
- `pnpm turbo run typecheck test lint` from root — Plan 01's three gates still green WITH apps/mcp now in the graph.
</verification>

<success_criteria>
- apps/mcp is a real Next 16 App Router app, distinguishable from the 9 stubs.
- /api/health is a working smoke endpoint ready for Plan 03 to curl after deploy.
- /api/mcp/[transport] is mounted via mcp-handler at the canonical path with zero tools (Phase 3 ready).
- Vitest sanity test gives FOUND-03's test gate something to run in apps/mcp.
- All three Turbo gates remain green from root after this plan.
- No Phase 2 (DB), Phase 3 (auth/tools), or Phase 4 (UI) scope leaked in.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-02-SUMMARY.md` documenting:
- Files created in apps/mcp.
- `next build` output summary (Turbopack confirmation).
- Vitest run output (1 passing).
- Local curl result against `http://localhost:3001/api/health`.
- Anything for Plan 03 to know (e.g., port chosen for local dev = 3001; path is `/api/health`; Vercel project Root Directory must be `apps/mcp`).
</output>
